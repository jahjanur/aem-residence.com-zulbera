# Copy to .env and set for production. In dev, Vite proxy forwards /api to backend.

# API base URL. Leave empty or set to /api when using Vite proxy (dev).
# In production, set to your API origin e.g. https://api.yourdomain.com
VITE_API_BASE=/api
